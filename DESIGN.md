# AI 语音绘图工具 — 设计开发方案

## 1. 项目概述

### 1.1 项目目标

开发一款纯语音控制的绘图工具。用户不能使用鼠标或键盘，仅通过语音指令完成图形绘制创作。系统需要准确理解用户的自然语言指令，将其转化为画布上的图形操作，并提供即时的视觉与语音反馈。

### 1.2 设计原则

- **稳字当头**：核心链路（语音识别 → 指令解析 → 绘图执行）必须极其稳定
- **分层递进**：基础指令绝对可靠，高级能力逐步叠加
- **高水平组装**：不做底层技术攻关，选择成熟开源库与云服务进行组装
- **工程量聚焦**：把精力集中在指令理解层和交互体验层

### 1.3 核心竞争力

指令理解层（LLM Function Calling + 指令系统设计）和交互体验层（语音状态可视化 + 容错纠错机制）是本项目的技术深度所在。

---

## 2. 技术栈选型

| 层级 | 选型 | 版本 | 选型理由 |
|------|------|------|---------|
| 前端框架 | Vite + React + TypeScript | Vite 6 / React 18 | Vite 极速开发体验；React 组件化解耦语音层、绘图层、UI 层；TypeScript 为指令系统提供强类型保障 |
| 绘图引擎 | Fabric.js | v6 | 对象化画布模型，天然适合程序指令控制 |
| 语音识别 | Web Speech API（主）/ Whisper API（备） | 浏览器原生 | 零成本、实时流式、低延迟 |
| 指令解析 | OpenAI 兼容 API + 多模型故障转移 | — | Function Calling 输出严格结构化 |
| 语音反馈 | Web Speech Synthesis API | 浏览器原生 | 零成本、零配置 |

### 2.1 绘图引擎选型详细对比

| 维度 | Fabric.js ✅ | Konva.js | 原生 Canvas API |
|------|------------|----------|----------------|
| 对象管理 | 内建完整对象模型，支持选择、分组、z-index | 需要 Layer/Group 手动管理 | 无，需完全自行实现 |
| 序列化 | toJSON() / loadFromJSON() 原生支持 | 需要自行实现 | 不适用 |
| 编程式操作 | 通过 API 直接创建/修改/删除对象 | 类似 | 每次操作需手动重绘 |
| 撤销/重做 | 可基于 JSON 快照轻松实现 | 可行但更复杂 | 极其困难 |
| 图形类型 | 矩形、圆、椭圆、三角形、多边形、路径、文本、图片 | 类似，路径支持较弱 | 需逐个手写 |

**结论**：Fabric.js 的"对象化画布"理念意味着画布上每个图形都是独立 JS 对象，天然适合通过程序指令（语音解析后的结构化命令）来控制。

### 2.2 语音识别方案对比

| 维度 | Web Speech API（主） | Whisper API（备） |
|------|---------------------|------------------|
| 成本 | 零 | 按音频时长计费 |
| 延迟 | 200-500ms（流式） | 1-3s（需录制上传） |
| 准确率 | 安静环境 > 95% | 嘈杂环境更优 |
| 适用场景 | 演示环境良好时 | 现场噪音大时 |

---

## 3. LLM 集成方案（多模型故障转移）

### 3.1 模型提供商与优先级

| 优先级 | 提供商 | 模型 | API Base URL | 选型理由 |
|--------|--------|------|-------------|---------|
| P0 | DeepSeek | deepseek-chat | `https://api.deepseek.com` | API 格式完全兼容 OpenAI，零迁移成本；function calling 稳定；价格极低 |
| P1 | 通义千问 | qwen-max | `https://dashscope.aliyuncs.com/compatible-mode/v1` | 中文理解能力国内顶尖；OpenAI 兼容模式降低适配成本 |
| P2 | 智谱 GLM | glm-4-flash | `https://open.bigmodel.cn/api/paas/v4` | 有免费额度，作为兜底方案 |

### 3.2 故障转移策略

```
请求发出
  │
  ▼
DeepSeek API 调用 ──成功──→ 返回结果
  │ 失败（网络超时 / API 错误 / 速率限制）
  ▼
通义千问 API 调用 ──成功──→ 返回结果
  │ 失败
  ▼
智谱 GLM API 调用 ──成功──→ 返回结果
  │ 失败
  ▼
返回友好错误 → 前端提示用户重试
```

实现方式：统一的 `LLMService` 类封装所有提供商调用，使用 OpenAI SDK 兼容格式。每次请求按优先级依次尝试，当前提供商失败时自动降级到下一个。记录失败原因用于调试。

### 3.3 Function Calling 工具设计

> 更新于 2026-06-12：新增 line（线段）、star（五角星）图形类型，新增 group_id 支持复合对象。

系统定义以下工具供 LLM 调用：

**create_shape** — 创建图形
- shape_type: circle / rectangle / square / triangle / ellipse / line / star
- color: 填充颜色（hex 或颜色名），线段类型使用 stroke_color
- center_x, center_y: 中心坐标（0-800 / 0-600 逻辑坐标系）
- size: 尺寸（圆形为半径，矩形为边长，星形为外径）
- stroke_color: 边框颜色（可选），线段类型中为线段颜色
- stroke_width: 边框/线段宽度（可选）
- start_x, start_y, end_x, end_y: 线段起止坐标（仅 line 类型）
- opacity: 透明度 0-1（可选，默认 1）
- group_id: 分组 ID，复合对象的所有子图形共享同一 group_id（可选）

**modify_shape** — 修改已有图形
- target_id: 目标对象 ID（从画布状态上下文中获取）
- color: 新颜色（可选）
- center_x, center_y: 新位置（可选）
- scale: 缩放倍数（可选）
- rotation: 旋转角度（可选）
- opacity: 透明度（可选）

**delete_shape** — 删除图形
- target_id: 目标对象 ID

**clear_canvas** — 清空画布（无参数）

**query_canvas** — 查询画布状态
- query_type: count / largest / smallest / colors / by_type

### 3.4 坐标映射策略

> 更新于 2026-06-12：画布改为响应式设计，坐标系统相应调整。

**方案：LLM 直出像素坐标 + 前端按比例映射 + 后处理修正**

- System Prompt 中告知 LLM 逻辑画布尺寸（800×600）作为参考坐标系
- LLM 输出基于此逻辑坐标系的绝对像素值
- 前端按实际画布尺寸做比例映射：`实际X = (LLM输出X / 800) * 画布实际宽度`
- 前端做一层后处理修正：
  - **网格吸附**：坐标接近画布中心/四分点/边缘时自动对齐
  - **边界修正**：确保图形不会超出画布范围
  - **最小尺寸保护**：防止 LLM 输出过小的尺寸

**优势**：LLM 始终使用固定的逻辑坐标系思考，前端负责适配不同窗口尺寸。

### 3.5 目标定位策略

每次请求 LLM 时，将当前画布状态注入 System Prompt：

```
当前画布对象:
[
  { id: "obj_1", type: "circle", color: "#FF0000", x: 400, y: 300, radius: 80 },
  { id: "obj_2", type: "rectangle", color: "#0000FF", x: 200, y: 150, width: 120, height: 80 }
]
```

LLM 通过对象 ID 引用目标。例如用户说"把红色的圆改成蓝色"，LLM 看到上下文中 obj_1 是红色圆形，在 tool call 中输出 `target_id: "obj_1"`。前端根据 ID 找到对象执行修改。

定位策略优先级：精确匹配（颜色+形状）→ 最近创建 → 模糊描述（最大/最左）→ 确认机制（匹配不唯一时询问）。

---

## 4. 指令系统设计

### 4.1 指令层级

**L0 — 画布控制指令（关键词匹配，不走 LLM，延迟 < 50ms）**

| 指令 | 操作 |
|------|------|
| "清空画布" | canvas.clear() |
| "撤销" / "回退" | undo() |
| "重做" | redo() |

**L1 — 基础绘图指令（LLM Function Calling，核心必保）**

| 指令类型 | 示例 | 对应工具 |
|---------|------|---------|
| 创建 | "画一个红色的大圆形" | create_shape |
| 创建+定位 | "在中间画一个蓝色矩形" | create_shape |
| 修改颜色 | "把红色的圆改成蓝色" | modify_shape |
| 修改大小 | "把矩形放大两倍" | modify_shape |
| 修改位置 | "把三角形移到右上角" | modify_shape |
| 删除 | "删掉最后一个图形" | delete_shape |

**L2 — 复合与空间指令（Phase 2 实现）**

| 指令类型 | 示例 | 处理方式 |
|---------|------|---------|
| 复合 | "画一个红圆和一个蓝方形" | LLM 返回多个 tool call |
| 空间关系 | "在圆形右边画一个方形" | LLM 计算相对坐标 |
| 查询 | "画布上有几个图形" | query_canvas |

**L3 — 智能推理指令（Phase 4 可选）**

| 指令类型 | 示例 | 处理方式 |
|---------|------|---------|
| 模糊理解 | "画一个好看的房子" | LLM 组合多个基础图形 |
| 风格变换 | "用暖色调重绘所有图形" | 批量 modify_shape |

### 4.2 复合对象绘制方案

> 更新于 2026-06-12：Phase 1 实现基于 prompt scaffolding 的复合对象分解绘制。

**业内调研**：参考 Chat2SVG（arXiv 2411.16602）的"展开→分解→定位"三步法，以及 Symbolic Graphics Programming（arXiv 2509.05208）的锚点定位策略。

**方案：LLM 多次 create_shape + system prompt 分解引导**

- 不新增 `draw_composite` 工具（灵活性不足，维护成本高）
- 在 system prompt 中提供分解规则和参考示例（few-shot prompting）
- LLM 根据锚点定位策略，将复合对象分解为 3-12 个基本图形
- 所有子图形使用相同的 `group_id`，方便后续批量操作
- 从后往前调用 create_shape（先背景后前景，保证正确 z-ordering）

**参考分解模板**（写入 system prompt）：

| 对象 | 分解方式 |
|------|---------|
| 房子 | 矩形(墙) + 三角形(顶) + 小矩形(门/窗/烟囱) |
| 太阳 | 圆形(本体,黄色) + 多条线段(光芒) |
| 花朵 | 圆形(花蕊) + 椭圆×N(花瓣) + 线段(茎) + 椭圆(叶) |
| 树 | 矩形(树干) + 椭圆/圆形×N(树冠) |
| 小狗 | 椭圆(身) + 圆形(头) + 椭圆×2(耳) + 线段×4(腿) + 线段(尾) |
| 汽车 | 矩形(车身) + 小矩形(窗) + 圆形×2(轮) |

---

## 5. 语音交互设计

### 5.1 语音状态可视化

| 状态 | 视觉反馈 | 说明 |
|------|---------|------|
| 待机 | 呼吸灯效果麦克风图标 | 系统正在监听 |
| 识别中 | 波形动画 + 实时字幕 | 正在识别语音 |
| 思考中 | 加载动画 + "正在理解..." | LLM 正在解析指令 |
| 执行成功 | 绿色勾号 + 图形出现动画 | 绘图完成 |
| 执行失败 | 红色提示 + TTS 引导 | 引导用户重新表述 |

### 5.2 确认机制

破坏性操作（清空画布、批量删除）需二次确认：TTS 播报"确定要清空画布吗？说'确定'来确认"。

### 5.3 指令历史面板

屏幕右侧固定 280px 侧栏显示指令历史，每条包含：用户说了什么 → 系统理解为什么 → 执行了什么操作。侧栏内独立滚动，不影响画布区域。演示时是极好的佐证，证明系统在"理解"而非"猜测"。

### 5.4 界面布局设计

> 更新于 2026-06-12：经实际测试后重新设计布局，解决指令历史挤压画布的问题。

采用"左画布 + 右侧栏"分栏布局，确保各元素互不干扰：

```
┌──────────────────────────────────────────────┐
│  AI 语音绘图工具              (48px 标题栏)   │
├──────────────────────────┬───────────────────┤
│                          │  🎤 语音控制      │
│                          │  (反馈信息)       │
│      画布区域            │───────────────────│
│      (自适应填充)        │  指令历史         │
│                          │  (独立滚动)       │
├──────────────────────────┤  ✅ "画圆" → ...  │
│ 试试说: [画圆] [矩形]   │  ❌ "xxx" → ...   │
└──────────────────────────┴───────────────────┘
```

- **画布区域**：flex: 1 自适应填充，使用 ResizeObserver 检测容器尺寸，图形尺寸按比例计算
- **右侧栏**：固定 280px 宽度，包含语音控制卡片和指令历史面板
- **指令历史**：flex: 1 占满侧栏剩余空间，内部 overflow-y: auto 独立滚动
- **提示条**：画布底部，展示可用指令示例

### 5.5 语音识别同音字纠错

> 更新于 2026-06-12：Phase 1 实现双层纠错架构，取代 Phase 0 的简单映射方案。

**问题**：Web Speech API 中文识别同音字错误率约 8-15%，如"梯形"→"T型"、"矩形"→"举行"、"圆形"→"园型"。

**业内调研结论（2026-06）**：

| 方案 | 延迟 | 成本 | 准确率提升 | 推荐度 |
|------|------|------|-----------|--------|
| 领域词典 + pinyin-pro 拼音匹配 | ~5ms | 免费 | 高（已知词 90%+） | 主力层 |
| LLM system prompt 上下文纠错 | +0ms（复用） | ~0 额外 | 中高 | 辅助层 |
| Whisper WASM 浏览器端 | 500ms+ | 免费 | 低（小模型中文差） | 不推荐 |
| SenseVoice/FunASR 服务端 | 200-500ms | 服务器成本 | 高 | 过重 |
| ChineseErrorCorrector4 (4B) | 200-300ms | GPU 服务器 | 最高 | 杀鸡用牛刀 |

**Phase 1 双层架构**：

- **Layer 1（客户端词典纠错，~5ms）**：
  - 使用 `pinyin-pro` 库将文本转拼音，与领域词汇库做滑动窗口匹配
  - 精确替换表覆盖形状名（梯形/矩形/圆形/三角形等）、颜色、位置、动作词的同音字
  - 模式替换覆盖一类规律（如"X型"→"X形"、"X涩"→"X色"）
  - 实现文件：`src/services/textCorrection.ts`

- **Layer 2（LLM 上下文纠错，+0ms 延迟）**：
  - 在 system prompt 中列出常见同音字错误映射
  - LLM 根据上下文自动理解正确意图，不额外告知用户纠正了什么
  - 复用现有 LLM 调用，不增加额外 API 请求

**为何不用 Phase 0 简单映射**：简单映射无法覆盖所有变体，且需要不断人工维护。双层架构中 Layer 1 覆盖高频已知错误，Layer 2（LLM）处理剩余边界情况，实现接近 100% 的纠错率。

---

## 6. 系统架构

```
用户语音
   │
   ▼
┌─────────────────────────┐
│     Voice Engine        │  Web Speech API / Whisper
│     语音识别层           │  → 返回识别文本
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│   L0 指令快判（正则匹配） │  命中 → 直接执行，不走 LLM
└────────┬────────────────┘
         │ 未命中
         ▼
┌─────────────────────────┐
│   Command Parser        │  LLM Function Calling（多模型故障转移）
│   指令解析引擎           │  输入：识别文本 + 画布状态
│                         │  输出：结构化绘图指令[]
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│   Command Executor      │  参数校验 + 坐标修正 + 目标定位
│   + Post-Processing     │  → 执行 Fabric.js API 调用
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│   Drawing Engine        │  Fabric.js v6
│   绘图引擎层             │  → 画布操作 + 动画反馈
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│   Feedback Layer        │  视觉动画 + TTS 语音反馈
│   反馈层                 │  + 指令历史面板更新
└─────────────────────────┘
```

### 6.1 调试日志系统

> 更新于 2026-06-12：Phase 1 新增结构化日志。

所有日志使用统一前缀 `[AI-Draw]`，方便在浏览器 DevTools 中过滤。

| 日志标签 | 内容 | 触发时机 |
|---------|------|---------|
| `[语音识别]` | ASR 原始文本 | 每次收到 final result |
| `[文本纠错]` | 纠正前后对比 | 仅在发生纠正时 |
| `[LLM请求]` | 画布状态（JSON） | 每次调用 LLM 前 |
| `[LLM响应]` | 模型提供商、消息内容、每个工具调用的函数名和参数 | LLM 返回后 |
| `[执行结果]` | 工具执行汇总 | 所有工具调用执行完毕 |
| `[LLM调用]` | 完整错误信息（name/message/stack/cause） | LLM 调用失败时 |

---

## 7. 分阶段开发计划

### Phase 0：脚手架搭建（0.5 天）✅ 已完成

**目标**：搭建项目骨架，跑通"说话 → 出字 → 画图"端到端链路。

**已完成工作**：
- ✅ 初始化 Vite + React + TypeScript 项目
- ✅ 集成 Fabric.js v6，创建响应式画布（自适应容器尺寸）
- ✅ 接入 Web Speech API，实现点击按钮说话并实时显示识别文字
- ✅ 关键词匹配绘图：识别"画圆/画方/画三角"等指令并绘制
- ✅ 同音字纠错映射表（临时方案）
- ✅ 左画布 + 右侧栏分栏布局，指令历史独立滚动
- ✅ Vite dev proxy 配置好三个 LLM 提供商转发

**项目结构**：
```
ai-draw/
├── DESIGN.md                    # 本文档
├── package.json / vite.config.ts / tsconfig.json
├── .env.example                 # API Key 模板
├── src/
│   ├── App.tsx / App.css        # 主组件 + 布局
│   ├── types/drawing.ts         # 类型定义
│   ├── components/
│   │   ├── CanvasPanel.tsx      # Fabric.js 画布封装
│   │   ├── VoiceButton.tsx      # 语音按钮
│   │   └── CommandHistory.tsx   # 指令历史
│   └── services/
│       ├── speech.ts            # 语音识别服务
│       └── llm.ts               # 多模型故障转移服务
```

### Phase 1：核心链路打通（2-3 天）✅ 进行中

**目标**：跑通完整的"语音 → LLM 解析 → 绘图执行"链路，支持 L0 + L1 基础指令。

**已完成工作**：
- ✅ LLM Function Calling 集成（OpenAI SDK + 多模型故障转移）
- ✅ L0 快速指令（清空、撤销）—— 关键词匹配，不走 LLM
- ✅ L1 创建/修改/删除类指令
- ✅ 画布状态注入 system prompt（目标定位）
- ✅ 语音状态可视化（idle/listening/processing/executing/error）
- ✅ 双层 ASR 同音字纠错（pinyin-pro 词典 + LLM 上下文）
- ✅ 新增 line（线段）、star（五角星）图形类型
- ✅ 复合对象绘制支持（group_id + prompt scaffolding 分解规则）
- ✅ 完整调试日志（识别结果、纠错结果、LLM 请求/响应/错误）
- ✅ 修复 baseURL 相对路径导致 URL 构造失败的问题

**待完成**：
- L1 指令实际测试验证（成功率 > 90%）
- 端到端延迟测试（目标 < 3 秒）

### Phase 2：智能增强（2-3 天）✅ 已完成

**目标**：支持复合指令和空间关系推理。

**已完成工作**：
- ✅ 空间关系指令推理（"在圆形右边画矩形"、"排成一排"等相对定位）
- ✅ 查询指令完善：count / largest / smallest / colors / by_type 五种查询类型
- ✅ 目标确认机制：破坏性操作（清空画布、批量删除）的语音+按钮二次确认，15秒超时自动取消
- ✅ 批量删除支持：delete_shape 工具新增 filter_type / filter_color / all 过滤参数
- ✅ 绘图入场动画：图形创建时 scale 从 0 到 1 的 easeOutCubic 动画，线段使用 opacity 淡入
- ✅ TTS 语音反馈：查询结果语音播报、确认提示语音播报、错误提示语音播报
- ✅ 指令类型追踪：指令历史显示真实命令类型（create_shape / modify_shape / composite 等）
- ✅ 画布状态增强：getCanvasState 返回 width/height 信息，辅助 LLM 空间推理
- ✅ 新增 CanvasPanel 方法：getLargestShape / getSmallestShape / getShapesByType / getCanvasSize

**验收标准**：
- 复合指令正确拆解并依次执行
- 空间关系指令正确计算相对位置（±20px 偏差）
- 指令历史面板完整记录每条指令

### Phase 3：体验打磨（2 天）

**目标**：针对比赛演示场景专项优化。

**具体工作**：
- Whisper API 备选通道 + 自动降级
- 延迟体验优化（流式识别显示 + LLM 流式输出）
- 演示模式设计
- 错误恢复机制
- UI 美化

**验收标准**：
- 演示脚本可连续执行 15+ 条指令无中断
- 错误优雅恢复，不会卡死或白屏
- 整体演示 3-5 分钟内完成

### Phase 4（可选）：高阶能力（1-2 天）

方向 A — 场景化绘图（"画一个交通信号灯" → 自动组合图形）

方向 B — 智能纠错引导（"画一个图形" → "您想画什么形状呢？"）

### 时间线总览

| 阶段 | 时间 | 核心产出 | 优先级 |
|------|------|---------|--------|
| Phase 0 | 0.5 天 | 项目骨架 + 端到端 Hello World | 必做 |
| Phase 1 | 2-3 天 | L0+L1 全部指令 + 语音状态 UI + 多模型故障转移 | 必做 |
| Phase 2 | 2-3 天 | L2 复合/空间/查询指令 + 历史面板 | 必做 |
| Phase 3 | 2 天 | 演示优化 + 容错 + 备选通道 | 必做 |
| Phase 4 | 1-2 天 | L3 场景化绘图或智能引导（二选一）| 选做 |

---

## 8. 风险控制

| 风险 | 应对方案 |
|------|---------|
| 现场网络不稳定 | 本地缓存常见指令解析结果；准备手机热点 |
| 现场噪音导致识别率低 | 切换 Whisper API 模式；准备外置降噪麦克风 |
| 大模型 API 不可用 | 多模型故障转移机制（DeepSeek → 千问 → 智谱） |
| 复合指令解析错误 | 指令历史面板展示系统理解；用撤销回退 |
| API Key 泄露 | Vite dev proxy 转发，Key 仅在 .env 文件中 |

---

## 9. 演示脚本（3 分钟参考）

```
第一步 — 基础创建（L1）：
  "画一个大的蓝色圆形在画布中间"         → 蓝色大圆出现
  "在圆形的右边画一个红色正方形"         → 红色方形出现在右侧
  "在左下角画一个绿色三角形"             → 绿色三角出现

第二步 — 属性修改（L1）：
  "把蓝色的圆改成橙色"                   → 圆形变色
  "把正方形缩小一半"                     → 方形缩小

第三步 — 复合指令（L2）：
  "在上面再画三个不同颜色的小圆形排成一排" → 三个圆自动排列

第四步 — 查询（L2）：
  "画布上一共有几个图形"                  → TTS/面板显示数量

第五步 — 撤销与容错：
  "撤销上一步"                           → 操作撤销
  "清空画布"                             → 确认 → 清空

第六步 — 场景化绘图（L3，可选）：
  "画一个简易的交通信号灯"                → 自动组合矩形+三个圆形+颜色
```
